module Example exposing (..)

import Expect exposing (Expectation)
import Fuzz exposing (Fuzzer, int, list, string)
import Test exposing (..)


submitForm : Test
submitForm =
    describe "button"
        [ test "When click submit button an error message is diplayed" <|
            \_ -> 
          
        , test "When click submit button a success message is displayed" <|
            \_ -> 
     
        ]


submitForm : Test
submitForm =
    describe "input"
        [ test "check that the onclick togglenotification works" <|
            \_ -> 
        ]


